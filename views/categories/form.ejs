<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/styles.css" />
    <link rel="stylesheet" href="/css/forms.css" />
    <link rel="icon" type="image/x-icon" href="/images/logo-plantory.ico">
    <title><%= category ? "Edit Category" : "Create Category" %></title>
  </head>

  <body>
    <%- include("../partials/header") %>

    <div class="wrapper">
      <main class="main-content">
        <h2 class="main-title">
          <%= category ? "Edit Category" : "Create Category" %>
        </h2>

        <form
          action="<%= category ? '/categories/edit/' + category.id : '/categories/create' %>"
          method="POST"
          class="form-container"
        >
          <div class="form-item">
            <label for="name" class="form-name">Category Name:</label>
            <input
              type="text"
              name="name"
              value="<%= category ? category.name : '' %>"
              class="form-input"
              maxlength="20"
              required
            />
          </div>

          <div class="form-item">
            <label for="description" class="form-name">Description:</label>
            <textarea name="description" class="form-input" maxlength="200"><%= category ? category.description : '' %></textarea>
          </div>

          <div class="btn-action-container">
            <button type="submit" class="btn"><%= category ? "Update" : "Create" %></button>
            <a href="/categories" class="btn">Cancel</a>
          </div>
        </form>
      </main>
    </div>

    <%- include("../partials/footer") %>
  </body>
</html>
