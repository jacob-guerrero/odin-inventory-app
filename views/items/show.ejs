<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/styles.css" />
    <link rel="stylesheet" href="/css/sections.css" />
    <link rel="stylesheet" href="/css/show.css" />
    <link
      rel="stylesheet"
      href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined"
    />
    <link rel="icon" type="image/png" href="/images/logo-plantory.png">
    <title>Item - <%= item.name %></title>
  </head>

  <body>
    <%- include("../partials/header") %>

    <main class="main-content">
      <h2 class="main-title"><%= item.name %></h2>

      <section class="text-container">
        <p class="plain-text">
          <strong>Category:</strong>
          <%= category.name %>
          <a href="/categories/<%= item.category_id %>">View More</a>
        </p>
        <p class="plain-text"><strong>Price:</strong> $<%= item.price %></p>
        <p class="plain-text"><strong>Stock Quantity:</strong> <%= item.stock_quantity %></p>
        <p class="plain-text"><strong>Description:</strong> <%= item.description %></p>

        <div class="btn-action-container">
          <a
            href="/items/edit/<%= item.id %>"
            class="btn cs-button-solid"
            title="Edit"
            ><span class="material-symbols-outlined"> edit_square </span></a
          >
          <form
            action="/items/delete/<%= item.id %>"
            method="POST"
            class="btn-delete-container"
          >
            <button
              type="submit"
              onclick="return confirm('Are you sure?')"
              class="btn cs-button-solid btn-delete"
              title="Delete"
            >
              <span class="material-symbols-outlined"> delete </span>
            </button>
          </form>
        </div>
      </section>

      <aside class="animation <%= category.name === "Indoor Plants" ? "indoor" : category.name === "Outdoor Plants" ? "outdoor" : "other" %> ">
        
      </aside>
    </main>

    <%- include("../partials/footer") %>
  </body>
</html>
